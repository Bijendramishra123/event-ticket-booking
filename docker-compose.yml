services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: event-booking-backend
    restart: unless-stopped
    ports:
      - "5001:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGODB_URI=mongodb+srv://bijendramishra2002_db_user:Kd831946@officepantrycluster.b7d7cmw.mongodb.net/event_ticket_booking?retryWrites=true&w=majority
      - JJWT_SECRET=ev3ntB00k1ngS3cr3tK3y@2026#Secur3$Tr0ng!P@ssw0rd
      - JWT_EXPIRE=7d
      - FRONTEND_URL=http://localhost:8080
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: event-booking-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://backend:5000/api
    networks:
      - app-network

networks:
  app-network:
    driver: bridge